node_modules/
.next/
out/
dist/
build/

.env
.env.local
.env.*.local

treasury-keypair.json
treasury-keypair*.json

runs/*
!runs/.gitkeep

# Data-root subtrees (file-system run artifacts, payment counters, faucet
# claim log). Anchored with a leading slash so we don't accidentally
# ignore files like `apps/web/app/api/users` or `apps/web/app/admin/users`.
/users/
/payments/
/faucet-claims.json

*.log
.DS_Store
.turbo/
.vercel/
.playwright-mcp/

packages/zerion-fork/skills/*/node_modules/

.pnpm-store/
.vercel
